{% extends 'base.html.twig' %}

{% block title %}PaymentForm{% endblock %}

{% block body %}
    <h1>PaymentForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_form.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ payment_form.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payment_form_index') }}">back to list</a>

    <a href="{{ path('payment_form_edit', {'id': payment_form.id}) }}">edit</a>

    {{ include('payment_form/_delete_form.html.twig') }}
{% endblock %}
