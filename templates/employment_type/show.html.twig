{% extends 'base.html.twig' %}

{% block title %}EmploymentType{% endblock %}

{% block body %}
    <h1>EmploymentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employment_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ employment_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('employment_type_index') }}">back to list</a>

    <a href="{{ path('employment_type_edit', {'id': employment_type.id}) }}">edit</a>

    {{ include('employment_type/_delete_form.html.twig') }}
{% endblock %}
