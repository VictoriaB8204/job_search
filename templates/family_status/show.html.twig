{% extends 'base.html.twig' %}

{% block title %}FamilyStatus{% endblock %}

{% block body %}
    <h1>FamilyStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ family_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('family_status_index') }}">back to list</a>

    <a href="{{ path('family_status_edit', {'id': family_status.id}) }}">edit</a>

    {{ include('family_status/_delete_form.html.twig') }}
{% endblock %}
