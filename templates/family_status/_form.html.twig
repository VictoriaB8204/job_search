{{ form_start(form) }}
    {{ form_row(form.name , {'row_attr':{'class':'form-group'}, 'attr' : {'class': 'form-control' }, 'label' : "Название семейного положения"}) }}
<button class="btn">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}
